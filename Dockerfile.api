# Minimal test — verify Render can run Docker at all
FROM alpine:3.18
RUN echo "=== Docker build OK on Render ===" && uname -a
EXPOSE 8090
CMD ["sh", "-c", "echo 'Container running' && sleep infinity"]
